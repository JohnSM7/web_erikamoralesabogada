---
import Layout from '../../layouts/Layout.astro';
import t from '../../i18n/fr.json';
---
<Layout
    title="Domaines de Pratique | Erika Morales Avocate à Fuengirola"
    description="Spécialiste en droit des étrangers, droit civil, de la famille, droit pénal et droit du travail sur la Costa del Sol. J'offre des conseils et une représentation juridique experte."
    lang="fr"
>
  <div class="page-header">
    <div class="container">
      <h1>{t.servicespage.title}</h1>
      <p>{t.servicespage.subtitle}</p>
    </div>
  </div>

  <section class="section">
    <div class="container services-grid">
      
      <div class="service-card">
        <h3>{t.servicespage.civilTitle}</h3>
        <p>{t.servicespage.civilText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.familyTitle}</h3>
        <p>{t.servicespage.familyText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.penalTitle}</h3>
        <p>{t.servicespage.penalText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.immigrationTitle}</h3>
        <p>{t.servicespage.immigrationText}</p>
      </div>

    </div>
  </section>
</Layout>

<style>
  .page-header {
    background-color: var(--color-light-gray);
    padding: 3rem 0;
    text-align: center;
    border-bottom: 1px solid #ddd;
  }
  .page-header p {
    max-width: 600px;
    margin: 0 auto;
  }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Deux colonnes sur bureau */
    gap: 2rem;
  }
  .service-card {
    border: 1px solid #ddd;
    padding: 2rem;
    border-radius: 5px;
    transition: all 0.3s ease;
  }
  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.05);
    border-color: var(--color-accent);
  }
  .service-card h3 {
    border-bottom: 2px solid var(--color-accent);
    padding-bottom: 0.5rem;
    display: inline-block;
  }

  /* --- CORRECTION POUR MOBILES --- */
  @media (max-width: 768px) {
    .services-grid {
      /* On passe la grille à une seule colonne */
      grid-template-columns: 1fr;
    }
  }
</style>