---
import Layout from '../../layouts/Layout.astro';
import t from '../../i18n/en.json';
---
<Layout
title="Practice Areas | Erika Morales Lawyer in Fuengirola"
description="Specialist in immigration, civil, family, criminal, and labor law on the Costa del Sol. I offer expert legal advice and representation."    
lang="en"
>
  <div class="page-header">
    <div class="container">
      <h1>{t.servicespage.title}</h1>
      <p>{t.servicespage.subtitle}</p>
    </div>
  </div>

  <section class="section">
    <div class="container services-grid">
      
      <div class="service-card">
        <h3>{t.servicespage.civilTitle}</h3>
        <p>{t.servicespage.civilText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.familyTitle}</h3>
        <p>{t.servicespage.familyText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.penalTitle}</h3>
        <p>{t.servicespage.penalText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.immigrationTitle}</h3>
        <p>{t.servicespage.immigrationText}</p>
      </div>

    </div>
  </section>
</Layout>

<style>
  .page-header {
    background-color: var(--color-light-gray);
    padding: 3rem 0;
    text-align: center;
    border-bottom: 1px solid #ddd;
  }
  .page-header p {
    max-width: 600px;
    margin: 0 auto;
  }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Dos columnas en escritorio */
    gap: 2rem;
  }
  .service-card {
    border: 1px solid #ddd;
    padding: 2rem;
    border-radius: 5px;
    transition: all 0.3s ease;
  }
  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.05);
    border-color: var(--color-accent);
  }
  .service-card h3 {
    border-bottom: 2px solid var(--color-accent);
    padding-bottom: 0.5rem;
    display: inline-block;
  }

  /* --- CORRECCIÓN PARA MÓVILES --- */
  @media (max-width: 768px) {
    .services-grid {
        /* Cambiamos la rejilla a una sola columna */
        grid-template-columns: 1fr;
    }
  }
</style>