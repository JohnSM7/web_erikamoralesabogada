---
import Layout from '../../layouts/Layout.astro';
import t from '../../i18n/fr.json';
---
<Layout title="Erika Morales | Avocate" lang="fr">
    <div class="hero">
        <div class="container">
            <h1>{t.homepage.heroTitle}</h1>
            <p>{t.homepage.heroSubtitle}</p>
            <a href={`/fr/${t.slugs.contact}`} class="cta-button">{t.homepage.heroCta}</a>
        </div>
    </div>
    
    <section class="section section-light">
        <div class="container">
            <h2>Bienvenue</h2>
            <p>
                J'offre un service juridique proche et professionnel. Mon objectif est de fournir la meilleure stratégie et défense pour chaque cas, en cherchant toujours la solution la plus favorable pour mes clients. Parcourez le site pour en savoir plus sur mes domaines de spécialisation.
            </p>
        </div>
    </section>

</Layout>

<style>
    /* Estilos idénticos a los otros index.astro */
    .hero {
        background-color: var(--color-dark-blue);
        color: var(--color-white);
        padding: 6rem 0;
        text-align: center;
    }
    .hero h1 {
        color: var(--color-white);
        margin-bottom: 1.5rem;
    }
    .hero p {
        font-size: 1.2rem;
        max-width: 600px;
        margin: 0 auto 2rem auto;
    }
</style>