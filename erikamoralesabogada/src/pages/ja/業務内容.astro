---
import Layout from '../../layouts/Layout.astro';
import t from '../../i18n/ja.json';
---
<Layout
    title="取扱分野 | フエンヒローラの弁護士 エリカ・モラレス"
    description="コスタ・デル・ソルでの外国人法、民法、家族法、刑法、労働法を専門としています。専門的な法的アドバイスと代理業務を提供します。"
    lang="ja"
>
  <div class="page-header">
    <div class="container">
      <h1>{t.servicespage.title}</h1>
      <p>{t.servicespage.subtitle}</p>
    </div>
  </div>

  <section class="section">
    <div class="container services-grid">
      
      <div class="service-card">
        <h3>{t.servicespage.civilTitle}</h3>
        <p>{t.servicespage.civilText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.familyTitle}</h3>
        <p>{t.servicespage.familyText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.penalTitle}</h3>
        <p>{t.servicespage.penalText}</p>
      </div>

      <div class="service-card">
        <h3>{t.servicespage.immigrationTitle}</h3>
        <p>{t.servicespage.immigrationText}</p>
      </div>

    </div>
  </section>
</Layout>

<style>
  .page-header {
    background-color: var(--color-light-gray);
    padding: 3rem 0;
    text-align: center;
    border-bottom: 1px solid #ddd;
  }
  .page-header p {
    max-width: 600px;
    margin: 0 auto;
  }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* デスクトップでは2列 */
    gap: 2rem;
  }
  .service-card {
    border: 1px solid #ddd;
    padding: 2rem;
    border-radius: 5px;
    transition: all 0.3s ease;
  }
  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.05);
    border-color: var(--color-accent);
  }
  .service-card h3 {
    border-bottom: 2px solid var(--color-accent);
    padding-bottom: 0.5rem;
    display: inline-block;
  }

  /* --- モバイル向け修正 --- */
  @media (max-width: 768px) {
    .services-grid {
      /* グリッドを1列に変更 */
      grid-template-columns: 1fr;
    }
  }
</style>